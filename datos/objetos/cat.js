// //creo una variable cat con 3 propiedades
// let cat = {colour: "grey", name: "Spot", size: 46};
// // creo una varianle set con la propiedad "spot" que es true.
// let set = {"spot": true };
// // white fang" lo añado al set y que sea igual que true.
// set["white fang"] = true;
// // Elimino "spot"
// delete set["spot"];

// console.log("asaki" in set);
let mailArchive = retrieveMails ();
let livingCats = {"Spot": true};

for (let mail = 0; mail < mailArchive.length; mail++){
    let paragraph = mailArchive[mail].split("\n");
for (let paragraph = 0; paragraph < paragraph.length; paragraph++){
    if (startswith(paragraph[paragraph], "born")){
        let names = catnames
    }
}
}


